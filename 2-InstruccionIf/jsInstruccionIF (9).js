function mostrar()
{
	//Genero el número RANDOM entre 1 y 10 
	var numero;
	numero = parseInt(Math.random()*11-1)+1; //Manera de generar numero random ( Se puede remplazar el parse por Math.floor)
	alert (numero);
}//FIN DE LA FUNCIÓN